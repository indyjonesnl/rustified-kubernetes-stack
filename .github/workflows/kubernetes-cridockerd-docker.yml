name: kubernetes-cridockerd-docker

on:
  push:
    branches: [main]
    paths:
      - 'stacks/kubernetes-cridockerd-docker/**'
      - '.github/workflows/kubernetes-cridockerd-docker.yml'
  pull_request:
    paths:
      - 'stacks/kubernetes-cridockerd-docker/**'
      - '.github/workflows/kubernetes-cridockerd-docker.yml'
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Install minikube
        run: |
          curl -fsSLo minikube https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
          chmod +x minikube && sudo mv minikube /usr/local/bin/minikube
          minikube version

      - name: Run smoke (upstream K8s on cri-dockerd+Docker)
        run: make -C stacks/kubernetes-cridockerd-docker all
