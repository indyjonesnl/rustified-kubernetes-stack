name: kind-containerd-crun-coredns

on:
  push:
    branches: [main]
    paths:
      - 'stacks/kind-containerd-crun-coredns/**'
      - '.github/workflows/kind-containerd-crun-coredns.yml'
  pull_request:
    paths:
      - 'stacks/kind-containerd-crun-coredns/**'
      - '.github/workflows/kind-containerd-crun-coredns.yml'
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.31.0/kind-linux-amd64
          chmod +x ./kind && sudo mv ./kind /usr/local/bin/kind
          kind version

      - name: Install kubectl
        uses: azure/setup-kubectl@v4

      - name: Build node image, run cluster + smoke test
        run: make -C stacks/kind-containerd-crun-coredns all
